Unlike SASS variables, CSS custom properties 
-are scoped to the element they are declared on
-cascade
-can be manipulated in JavaScript. 
-don't need a preprocessor
-can be changed at runtime

HSL
more intuitive
simple to make color schemes that change programatically
incredibly easy to pick color variants

Why is HSL so much easier to use than RGB? Mainly due to the use of hue